# Disable optional dependencies that aren't needed for this project
optional=false

# Disable package-lock version warnings
fund=false

# Disable audit warnings for dev dependencies in production
audit-level=moderate